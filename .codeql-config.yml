name: "CodeQL Config"

paths:
  - src
  - tehkly-core/src
  - tehkly-frontend/src

paths-ignore:
  - node_modules
  - dist
  - build
  - coverage
  - '**/*.test.ts'
  - '**/*.spec.ts'

queries:
  - uses: security-and-quality
  - uses: security-extended

disable-default-queries: false
